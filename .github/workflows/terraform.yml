name: pipeline-terraform-aws

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
    Terraform_Plan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Comando
        run: echo "Teste Paulo Xavier"
      
